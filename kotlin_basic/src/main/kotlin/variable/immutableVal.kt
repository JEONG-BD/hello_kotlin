package variable

fun main(args : Array<String>) {
    val pi = 3.1416
    var radius = 10.0
    println(pi * pi * radius)

    //pi = 3.14 val cannot be reassigned error g
    println(pi * pi * radius)
}